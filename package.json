{
  "name": "ceuta-nos-une",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "db:local": "wrangler d1 migrations apply ceuta-nos-une --local",
    "db:remote": "wrangler d1 migrations apply ceuta-nos-une --remote"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "wrangler": "^4.127.0"
  }
}
